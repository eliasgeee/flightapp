<UserControl
    x:Class="FlightAppEliasGryp.Views.Flight.PlaneCard"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FlightAppEliasGryp.Views.Flight"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid Padding="24, 0, 24, 24">
        <Grid.ColumnDefinitions>
            <ColumnDefinition />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Spacing="10" Grid.Column="0" Grid.Row="1">
            <TextBlock Text="Maximum amount of seats"/>
            <TextBlock Text="Type" />
        </StackPanel>
        <StackPanel Spacing="10" Grid.Column="1" Grid.Row="1" Margin="24, 0, 0, 0">
            <TextBlock Text="{x:Bind ViewModel.FlightInfo.Flight.Plane.MaxAmountOfSeats}" />
            <TextBlock Text="{x:Bind ViewModel.FlightInfo.Flight.Plane.ToString()}"/>
        </StackPanel>
    </Grid>
</UserControl>
